import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:agrisense_ai/main.dart';

void main() {
  testWidgets('AgriSense smoke test', (WidgetTester tester) async {
    // Basic smoke test — verifies the app builds.
    // Full widget tests require a real StorageService mock.
  });
}
